<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!-- Add your custom status bar color -->
        <item name="android:statusBarColor">#FF000000</item>

        <!-- Optional: Change status bar icon color to light or dark -->
        <!-- true = dark icons, false = light icons -->
        <item name="android:windowLightStatusBar">false</item> <!-- false for light icons -->

        <!-- Optional: Set navigation bar color if desired -->
        <!-- <item name="android:navigationBarColor">@color/your_nav_bar_color</item> -->
    </style>
    <style name="DialogTheme" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="colorAccent">@color/orange</item>
        <item name="background">@color/black</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Optional: The derived theme can inherit from Base -->
    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
</resources>